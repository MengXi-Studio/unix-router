<template>
	<!-- #ifdef APP -->
	<scroll-view class="container">
	<!-- #endif -->
		<view class="page">
			<view class="hero">
				<text class="hero-title">unix-router</text>
				<text class="hero-sub">uni-app x 的 vue-router 风格路由库</text>
			</view>

			<view class="card" @click="go('/pages/query/query')">
				<text class="card-title">Query 传参</text>
				<text class="card-desc">push 携带 query，目标页 useRoute 读取</text>
			</view>
			<view class="card" @click="go('/pages/params/params')">
				<text class="card-title">Params 传参</text>
				<text class="card-desc">对象化 params（含中文 key）经 URL 传递</text>
			</view>
			<view class="card" @click="go('/pages/guard/guard')">
				<text class="card-title">导航守卫</text>
				<text class="card-desc">requireAuth 未登录跳转登录页</text>
			</view>
			<view class="card" @click="go('/pages/routerlink/routerlink')">
				<text class="card-title">RouterLink / useLink</text>
				<text class="card-desc">RouterLink 组件与激活态</text>
			</view>
			<view class="card" @click="go('/pages/test/test')">
				<text class="card-title">功能自检</text>
				<text class="card-desc">程序化断言核心 API，输出 PASS/FAIL</text>
			</view>

			<view class="footer">
				<text class="footer-text">当前路由: {{ currentPath }}</text>
			</view>
		</view>
	<!-- #ifdef APP -->
	</scroll-view>
	<!-- #endif -->
</template>

<script setup lang="uts">
import { useRouter, useRoute } from '@/uni_modules/ux-router/utssdk/index.uts'

const router = useRouter()
const route = useRoute()
const currentPath = ref(route.path)

function go(path: string): void {
	router.push(path)
}
</script>

<style>
.container {
	flex: 1;
}
.page {
	flex-direction: column;
	padding: 32rpx;
}
.hero {
	flex-direction: column;
	align-items: center;
	margin-bottom: 40rpx;
}
.hero-title {
	font-size: 40rpx;
	font-weight: bold;
	color: #007aff;
}
.hero-sub {
	font-size: 24rpx;
	color: #999;
	margin-top: 8rpx;
}
.card {
	flex-direction: column;
	background-color: #ffffff;
	border-radius: 12rpx;
	padding: 28rpx;
	margin-bottom: 24rpx;
	border-width: 1rpx;
	border-color: #eee;
}
.card-title {
	font-size: 30rpx;
	color: #333;
}
.card-desc {
	font-size: 24rpx;
	color: #999;
	margin-top: 8rpx;
}
.footer {
	margin-top: 32rpx;
	align-items: center;
}
.footer-text {
	font-size: 24rpx;
	color: #999;
}
</style>